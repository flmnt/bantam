class Auth {
  fetchAll(ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  fetchSingle(id, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  create(data, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  update(id, data, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  delete(id, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }
}

module.exports = Auth;
