class Index {
  fetchAll(ctx) {
    ctx.body = 'Index Fetch All';
  }

  fetchSingle(id, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  create(data, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  update(id, data, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }

  delete(id, ctx) {
    // ctx.body = 'YOUR RESPONSE';
  }
}

module.exports = Index;
